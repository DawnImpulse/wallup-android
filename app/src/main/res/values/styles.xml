<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:preferenceCategoryStyle">@style/TextAppearance.AppCompat.Body1</item>
    </style>

    <!--No action bar-->
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--Progress bar scrolling-->
    <style name="ScrollingProgressBar" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/white</item>
    </style>

    <!--Fullscreen-->
    <style name="AppTheme.Fullscreen" parent="AppTheme.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>


    <!--Preference Theme-->
    <style name="PrefsTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:colorBackground">@color/black</item>
        <item name="android:listViewStyle">@style/PrefsTheme.ListViewPrefs</item>
    </style>

    <!--Preference , List Theme-->
    <style name="PrefsTheme.ListViewPrefs" parent="Widget.AppCompat.ListView">
        <item name="android:background">@color/black</item>
    </style>

</resources>
